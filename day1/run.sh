# ||_____________________________________||
# || build type ||      shell command    ||
# ||____________||_______________________||
# || release    || ./code/dotnet run -c release ||
# || debugging  || ./code/dotnet run            ||
# ||_____________________________________||